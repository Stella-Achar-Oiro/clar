# Get this from https://dashboard.clerk.com → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...

# Backend URL — use / when frontend and backend are served together (production)
# Use http://localhost:8000 for local dev with separate servers
NEXT_PUBLIC_API_URL=http://localhost:8000
